# NucleAI runtime overrides — generated/overwritten by wren-ui LLMSettings.
# Loaded after .env in docker-compose, so values here win.
# Do NOT commit the real file (.env.runtime); it holds secrets in plain text.
# Copy this file to `.env.runtime` before the first `docker compose up`.
OPENAI_API_KEY=
